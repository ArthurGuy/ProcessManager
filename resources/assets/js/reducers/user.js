

const initialState = {isFetching: false, name: 'John Doe', avatar: null, locale: 'en-GB'}

//Process changes to pings
function user(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default user