

const initialState = {isFetching: false, items:[{name:'Hello'}], tags:[]}

//Process changes to pings
function contacts(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default contacts